// Модуль общей формы "РабочееМестоБригадировЗаказыНаСборку".
// На форме ожидаются:
// - реквизит формы "СписокЗаказов" типа ТаблицаЗначений;
// - таблица формы с тем же именем;
// - команды "Обновить", "ОткрытьЗаказ", "СохранитьКомментарий", "СнятьПометку".
// Если внутреннее имя документа отличается, замените его в процедуре ОткрытьЗаказ.

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

    ОбновитьСписокЗаказов();

КонецПроцедуры

&НаСервере
Процедура ОбновитьСписокЗаказов()

    СписокЗаказов = БригадирыЗаказыСервер.ПолучитьСписокЗаказовБригадиров();

КонецПроцедуры

&НаКлиенте
Процедура Обновить(Команда)

    ОбновитьНаСервере();

КонецПроцедуры

&НаСервере
Процедура ОбновитьНаСервере()

    ОбновитьСписокЗаказов();

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьЗаказ(Команда)

    ТекущиеДанные = Элементы.СписокЗаказов.ТекущиеДанные;
    Если ТекущиеДанные = Неопределено Тогда
        Возврат;
    КонецЕсли;

    ПараметрыФормы = Новый Структура("Ключ", ТекущиеДанные.Ссылка);
    ОткрытьФорму("Документ.ЗаказНаСборку.ФормаОбъекта", ПараметрыФормы);

КонецПроцедуры

&НаКлиенте
Процедура СохранитьКомментарий(Команда)

    ТекущиеДанные = Элементы.СписокЗаказов.ТекущиеДанные;
    Если ТекущиеДанные = Неопределено Тогда
        Возврат;
    КонецЕсли;

    ЗаписатьКомментарийБригадировНаСервере(
        ТекущиеДанные.Ссылка,
        ТекущиеДанные.КомментарийБригадиров
    );

    ОбновитьНаСервере();

КонецПроцедуры

&НаСервереБезКонтекста
Процедура ЗаписатьКомментарийБригадировНаСервере(СсылкаЗаказа, КомментарийБригадиров)

    БригадирыЗаказыСервер.ЗаписатьКомментарийБригадиров(СсылкаЗаказа, КомментарийБригадиров);

КонецПроцедуры

&НаКлиенте
Процедура СнятьПометку(Команда)

    ТекущиеДанные = Элементы.СписокЗаказов.ТекущиеДанные;
    Если ТекущиеДанные = Неопределено Тогда
        Возврат;
    КонецЕсли;

    СнятьПометкуБригадировНаСервере(ТекущиеДанные.Ссылка);
    ОбновитьНаСервере();

КонецПроцедуры

&НаСервереБезКонтекста
Процедура СнятьПометкуБригадировНаСервере(СсылкаЗаказа)

    БригадирыЗаказыСервер.СнятьПометкуБригадиров(СсылкаЗаказа);

КонецПроцедуры
